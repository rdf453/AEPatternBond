# PatternBond 최종 구현 플랜

## 1. 최종 목표

기존 `PatternGlueEntity` 기반의 유령 엔티티 구조를 폐기하고, 마스터 블록과 더미 블록으로 패턴 제공 시스템을 만든다.

```text
[Master Block] [Dummy Block] [Dummy Block] [Dummy Block]
```

- 마스터는 유일한 AE2 논리 Provider이자 채널 사용자다.
- 더미는 마스터의 확장 슬롯이며 실제 패턴 ItemStack을 소유한다.
- 마스터는 더미의 패턴을 논리 슬롯으로 매핑해 네트워크와 UI에 노출한다.
- 마스터는 요청을 예약하고 연결된 기계로 분배한다.
- 더미는 AE2 Grid 노드와 채널을 직접 만들지 않는다.
- 기존 AE2 Pattern Provider는 자동 변환하지 않고 독립적으로 공존한다.

## 2. 반드시 먼저 확인할 것

구현 전에 현재 Minecraft/NeoForge와 AE2 버전의 소스 또는 IDE 타입 정보를 확인한다.

1. 커스텀 BlockEntity가 AE2 Grid 메인 노드와 채널을 만드는 공식 방식.
2. 커스텀 Provider가 사용할 공식 인터페이스 또는 상속 지점.
3. AE2의 패턴 인벤토리 타입과 패턴 ItemStack 해석 방식.
4. 패턴 목록 노출, 작업 제출, 예약/바쁨 상태, 완료/실패 콜백.
5. 패턴 슬롯 변경 뒤 AE2 캐시를 갱신하는 방법.
6. 실제 패턴은 더미가 소유하고 마스터가 논리 Provider로 노출되는 구성이 공식 API로 가능한지.
7. 연결 기계로 작업을 전달하는 기능이 AE2 Provider 계약에 포함되는지.

공식 API가 허용하면 API와 어댑터만 사용한다. 허용하지 않는 경계만 Mixin 후보로 정하고, API 확인 전에 Mixin을 작성하지 않는다.

## 3. 기존 구조 정리

1. `Patternbond.java`의 모드 초기화와 이벤트 버스를 정리한다.
2. 블록, BlockEntityType, 아이템, 메뉴와 화면 등록 흐름을 만든다.
3. Gradle의 모드 ID와 `neoforge.mods.toml`의 모드 ID가 일치하는지 확인한다.
4. `EntityRegistry`와 `PatternGlueEntity` 등록 의존을 제거할 준비를 한다.
5. `PatternGlueEntity`의 AABB, spawn data, 엔티티 저장/동기화 구조는 새 시스템에서 재사용하지 않는다.
6. `PatternGlueItem`과 기존 슬롯 클래스는 새 마스터/더미 이벤트에 필요한 부분만 참고한다.

## 4. 마스터 블록

`PatternBondMasterBlock`과 `PatternBondMasterBlockEntity`를 만든다.

마스터 BlockEntity가 소유할 데이터:

- 연결된 더미의 차원과 `BlockPos` Set
- 논리 슬롯 매핑 목록
- 연결된 기계의 컨트롤러 위치와 접근 면
- 작업 예약과 실행 상태
- 데이터 포맷 버전
- 필요할 경우 소유자 정보

마스터의 책임:

- 더미 연결과 해제
- 논리 패턴 목록 생성
- UI 제공
- AE2 네트워크 Provider 역할
- 작업 예약과 기계 분배
- 더미와 기계의 유효성 재검증

## 5. 더미 블록

`PatternBondDummyBlock`과 `PatternBondDummyBlockEntity`를 만든다.

더미 BlockEntity가 소유할 데이터:

- 실제 패턴 ItemStack 슬롯
- 연결된 마스터의 차원과 위치
- 연결된 기계의 컨트롤러 위치와 접근 면
- 현재 연결 상태와 작업 상태

더미의 원칙:

- 패턴의 실제 소유자는 더미다.
- 마스터는 패턴을 복사하지 않고 더미 위치와 슬롯 번호만 참조한다.
- 더미는 AE2 Grid 노드와 채널을 만들지 않는다.
- 마스터가 파괴되어도 더미의 패턴은 유지한다.
- 기존 AE2 Provider를 더미로 교체하거나 나중에 복원하지 않는다.

## 6. 더미 생성과 연결

더미 생성은 다음 두 경우에만 허용한다.

- 마스터 설치 시
- 마스터 우클릭 시

연결 규칙:

- 빈 위치에만 더미를 생성한다.
- 기존 AE2 Provider나 다른 블록을 덮어쓰지 않는다.
- 동일한 더미를 여러 번 등록하지 않는다.
- 더미가 이미 다른 마스터에 연결되어 있으면 거부한다.
- 마스터가 없는 더미는 고아 상태로 두고 재연결/회수 규칙을 정한다.

기존 AE2 Provider 우클릭은 초기에는 변경하지 않는다. PatternBond 더미와 기존 AE2 Provider는 같은 월드에서 독립적으로 동작해야 한다.

## 7. 패턴 매핑과 마스터 UI

마스터에는 패턴 복사본이 아니라 출처 참조를 저장한다.

```text
논리 슬롯 0 -> 더미 A 위치, 슬롯 3
논리 슬롯 1 -> 더미 B 위치, 슬롯 0
논리 슬롯 2 -> 더미 A 위치, 슬롯 4
```

논리 슬롯에 필요한 정보:

- 더미 위치
- 더미 슬롯 번호
- 패턴 식별 정보
- 유효성 및 현재 상태

UI 규칙:

- 서버가 승인한 논리 슬롯만 표시한다.
- 슬롯 삽입, 추출, 이동은 실제 더미 슬롯에 대해 서버 검증 후 실행한다.
- UI용 ItemStack 복사본을 실제 저장 데이터로 취급하지 않는다.
- 더미 추가/삭제, 패턴 변경, 마스터 로드 때 논리 매핑을 갱신한다.
- 더미 우클릭은 연결된 마스터 UI를 연다.

## 8. 연결 기계 관리

방향이나 “몇 번째 기계”가 아니라 절대 위치로 관리한다.

```text
MachineBinding = 기계 컨트롤러 위치 + 접근 면 + 역할 + 상태
```

- 설치 또는 우클릭으로 연결 대상을 명시한다.
- 같은 방향에 여러 기계가 있어도 `BlockPos`로 구분한다.
- 멀티블록 기계는 부품 블록이 아니라 컨트롤러 위치를 저장한다.
- 기계 파괴, 교체, 청크 언로드 시 바인딩을 재검증한다.
- 마스터는 유효하고 예약되지 않은 기계만 작업 대상으로 선택한다.

## 9. 작업 스케줄러

마스터는 논리 패턴 하나를 네트워크에 노출하고, 여러 연결 기계로 요청을 분배한다.

```text
a 패턴 요청 3개
	-> 기계 1 예약
	-> 기계 2 예약
	-> 기계 3 예약
```

작업 상태:

```text
QUEUED -> RESERVED -> RUNNING -> COMPLETED
						 |
						 +-> FAILED
						 +-> CANCELLED
```

예약 규칙:

1. 논리 패턴에 대응하는 실행 가능한 기계를 찾는다.
2. 이미 예약된 기계와 유효하지 않은 기계를 제외한다.
3. 서버에서 기계를 원자적으로 예약한다.
4. 작업을 연결 기계에 전달한다.
5. 완료, 실패, 기계 제거, 청크 언로드 때 예약을 해제하거나 복구한다.

동일 패턴을 여러 기계가 처리하려면 각 기계가 실제로 병렬 실행 가능한지, 재료와 출력 공간이 있는지 확인한 뒤 분배한다.

## 10. AE2 통합과 Mixin

목표 구조:

```text
마스터 -> 유일한 Grid 노드와 채널
더미   -> Grid 노드 없음
```

1. 공식 AE2 확장 지점으로 마스터를 하나의 Provider로 등록한다.
2. 더미의 패턴 목록을 마스터의 논리 Provider에 매핑한다.
3. 패턴 변경, 더미 삭제, 마스터 로드 뒤 AE2 패턴 캐시를 갱신한다.
4. 더미가 패턴 제공자로 다시 노출되지 않는지 확인한다.
5. 공식 API로 가상 Provider가 불가능할 때만 최소 Mixin을 적용한다.
6. Mixin은 패턴 열거, Provider 등록, 변경 알림 등 필요한 한 지점씩 제한한다.
7. 단순히 UI에서 더미를 숨기는 방식으로 완료 처리하지 않는다. 예약과 작업 제출 경로에서도 더미가 중복 노출되지 않는지 확인한다.

## 11. 검증 순서

1. 모드 초기화와 등록만 완료한 뒤 `compileJava`를 통과시킨다.
2. 마스터를 배치했을 때 AE2 노드와 채널이 하나 생성되는지 확인한다.
3. 더미를 추가해도 채널 수가 증가하지 않는지 확인한다.
4. 더미 패턴을 저장하고 로드한 뒤 마스터 UI에 올바르게 표시되는지 확인한다.
5. 마스터 UI에서 조작한 슬롯이 실제 더미 슬롯만 변경하는지 확인한다.
6. 기존 AE2 Provider와 함께 배치해 기존 Provider의 채널과 패턴 목록이 변하지 않는지 확인한다.
7. 하나의 패턴 요청이 연결 기계에 전달되는지 확인한다.
8. 동일 패턴 여러 요청이 여러 기계로 분배되는지 확인한다.
9. 예약 충돌, 기계 파괴, 청크 언로드, 작업 실패를 확인한다.
10. 서버 재시작 후 더미-마스터 매핑과 작업 상태를 복구한다.
11. 마스터 파괴 후 더미 패턴이 남고 유효하지 않은 마스터 참조가 정리되는지 확인한다.
12. 모든 검증이 끝난 뒤 `PatternGlueEntity`와 `EntityRegistry`를 제거한다.

## 12. 명시적으로 제외할 것

- 공기나 흙에 BlockEntity 주입
- 기존 AE2 Provider를 PatternBond 블록으로 교체했다가 복원하는 방식
- 다른 모드 Provider 자동 판별
- 더미의 직접적인 AE2 Provider 등록
- 공식 API 확인 전 Mixin 작성
- 패턴을 마스터에 복사해 두는 이중 소유 구조
- 방향만으로 연결 기계를 추론하는 방식

## 최종 결정

- 실제 패턴 소유권은 더미가 가진다.
- 논리적 네트워크 소유권은 마스터가 가진다.
- 마스터만 채널을 소비한다.
- 더미는 마스터의 저장 공간과 확장 슬롯이다.
- 마스터는 논리 패턴 목록, 작업 예약, 기계 분배를 담당한다.
- 기존 AE2 Provider는 건드리지 않는다.
- `PatternGlueEntity`는 최종 구조에서 제거한다.
- AE2 API로 해결되지 않는 경계만 Mixin으로 다룬다.